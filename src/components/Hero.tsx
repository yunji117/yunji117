// src/components/Hero.tsx
const Hero = () => (
  <section id="hero" className="min-h-screen flex items-center justify-center">
    <h1 className="text-6xl font-bold opacity-0 animate-hero-fade">KIM YUNJI</h1>
  </section>
);

export default Hero;
